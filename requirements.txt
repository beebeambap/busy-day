# busy-day composer (phase 1)
mido>=1.3.0

# phase 2 (audio + score) — install when needed
# music21>=9.1
# midi2audio>=0.1.1            # requires fluidsynth (apt: fluidsynth)
# pillow>=10.0                 # post-process verovio output
# verovio>=4.3                 # MusicXML -> SVG (or use the CLI binary)
